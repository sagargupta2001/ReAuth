# Dev and Ops

## Local dev
- UI dev server: `npm run dev` (from `reauth/ui`)
- Backend run: `cargo run --package reauth_core --bin reauth_core`
- Embed UI: `cargo build --workspace` then `cargo run --package reauth_core --features embed-ui`

## Environments
- TODO

## Build and release
- TODO
